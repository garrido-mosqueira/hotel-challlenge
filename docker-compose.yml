services:

  price-challenge:
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ./price-api/target/price-api-1.1-SNAPSHOT.jar:/app.jar
    ports:
      - "8080:8080"
    container_name: price-challenge
